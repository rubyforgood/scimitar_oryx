<%= simple_form_for(@animal, multipart: true) do |f| %>
  <%= f.error_notification %>

  <%= f.input :name %>
  <%= f.input :species_id, collection: Species.all %>
  <%= f.input :facility_id, collection: Facility.all %>
  <%= f.label :image %>
  <%= f.file_field :image %>
  <br/>

  <%= f.button :submit %>
<% end %>
